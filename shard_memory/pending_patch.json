{
  "id": "c9c1dcfe-9bca-412a-8389-32e335ceab10",
  "timestamp": "2026-03-27T16:59:29.027652",
  "type": "refactor",
  "file": "backend/research_agenda.py",
  "description": "Avoids repeated calls to `self.capability_graph.get_missing_skills()` by caching the result.",
  "category": "performance",
  "rationale": "The `get_missing_skills()` method is called multiple times within the `choose_next_topic` function. Caching the result avoids redundant computation, especially if the underlying graph is large or the computation is expensive.",
  "philosophical_question": "",
  "changes": [
    {
      "old": "        # Priority 2: Goal gaps\n        if not topic_data and goal_prereqs:\n            try:\n                missing = self.capability_graph.get_missing_skills()\n                goal_missing = [s for s in missing if s in goal_prereqs]\n                if goal_missing:\n                    skill = random.choice(goal_missing)",
      "new": "        # Priority 2: Goal gaps\n        if not topic_data and goal_prereqs:\n            try:\n                missing = self.capability_graph.get_missing_skills()\n                if missing:\n                    goal_missing = [s for s in missing if s in goal_prereqs]\n                    if goal_missing:\n                        skill = random.choice(goal_missing)"
    },
    {
      "old": "        # Priority 4: Absolute Fallback (Random missing skill)\n        if not topic_data:\n            try:\n                missing = self.capability_graph.get_missing_skills()\n                if missing:\n                    skill = random.choice(missing)",
      "new": "        # Priority 4: Absolute Fallback (Random missing skill)\n        if not topic_data:\n            try:\n                if missing:\n                    skill = random.choice(missing)"
    }
  ],
  "status": "pending"
}