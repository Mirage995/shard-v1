# Python
__pycache__/
*.py[cod]
*$py.class
.env
venv/
.venv/
env/

# Node.js
node_modules/
dist/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Electron
dist-electron/
release/
out/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
.aider*

# SHARD runtime logs
shard_workspace/run_*.log
shard_workspace/strategy_quarantine.json
shard_workspace/swe_test/

# SHARD benchmark output artifacts (generated by gwt_*.py / mood_*.py / lobotomy_*.py)
shard_workspace/gwt_*.log
shard_workspace/gwt_*results*.json
shard_workspace/gwt_longitudinal_*.json
shard_workspace/gwt_multirun_*.json
shard_workspace/mood_*.log
shard_workspace/mood_*_results.json
shard_workspace/lobotomy_*.json
shard_workspace/strategic_*.json
shard_workspace/smoke_*.log
shard_workspace/stats.json
shard_workspace/longitudinal_snapshot/

# SHARD mood instrumentation history (local experimental data, not versioned)
shard_memory/mood_history.jsonl

# D2 frustration benchmark runtime artifacts (raw logs / per-run snapshots).
# Scientific summaries live under docs/experiments/ instead.
shard_workspace/d2_*.log
shard_workspace/d2_*.json
shard_workspace/d2_analysis_*.md
shard_workspace/d2_baseline_snapshot/
shard_workspace/d2_mood_history/

# Misc root-level junk created by sandbox cwd bug
/weight

# ── SHARD: Generated sandbox scripts (auto-created by NightRunner) ────────────
sandbox/
backend/sandbox/

# ── SHARD: Binary DB files (never commit live databases) ──────────────────────
shard_memory/*.db
shard_memory/*.db-shm
shard_memory/*.db-wal
shard_memory/*.bin
shard_memory/*.pickle
shard_memory/chroma.sqlite3

# ChromaDB vector stores (binary, not human-readable)
knowledge_db/
shard_memory/strategy_db/
shard_memory/0ed9c0af-*/
shard_memory/b9fafd48-*/
shard_memory/c7bca8ef-*/
backend/knowledge_db/

# ── SHARD: Backup files (temp, auto-generated) ────────────────────────────────
backend/*.bak_*
shard_memory/*.backup*
shard_memory/capability_graph.json.backup*
shard_memory/capability_graph.json.backup_norm*

# ── SHARD: Keep these JSON state files ────────────────────────────────────────
# (capability_graph.json, meta_learning.json, etc. are intentionally tracked)

# ── SHARD: Logs and reports (runtime output, not source) ──────────────────────
logs/
night_reports/
backend/logs/
backend/night_reports/

# ── SHARD: Dead code / archived ───────────────────────────────────────────────
old_attempts/
backups/

# ── SHARD: Local settings ─────────────────────────────────────────────────────
backend/reference.jpg
backend/settings.json
backend/last_cad_generation.json
settings.json
long_term_memory/
projects/
output.stl
cad_outputs/

# ── SHARD: Runtime state (ephemeral, not source) ───────────────────────────────
shard_memory/session.lock
shard_memory/voice_queue.json
shard_memory/voice_last_emit.json
shard_memory/persona_history.json
shard_memory/self_log.json
shard_memory/shard.db
shard_memory/shard.db-shm
shard_memory/shard.db-wal

# ── SHARD: Pitch/demo files ────────────────────────────────────────────────────
SHARD_Pitch_Deck.pdf
shard_pitch.html
MASTER_PROMPT_ANTIGRAVITY.md
run_vc_demo.py

# ── Local / CI artifacts ────────────────────────────────────────────────────────
.coverage
.claude/
test_inspect_live.py
test_patch_ui.py
test_popup.txt
test_swe.py
.shard_cache/

# ── SHARD: Root-level junk files (created by sandbox cwd bug) ─────────────────
# Patterns with shell-special chars ( ) { } ' ` were dropped because some
# tools (ripgrep, fd) emit warnings on unbalanced delimiters in gitignore.
# If the sandbox cwd bug recurs and produces a new junk filename, add a
# clean alphanumeric pattern here.
/max_fails
/str
/0
/3
/7.5
/0.65
/120s
/CERTIFY
/CONFIRMED
/None
/Dict
/Poi
/pass_rate
/runs

# ── SHARD: Exposed settings copies (never commit) ─────────────────────────────
settings*.json
settings\ -\ *.json
*\ -\ Copia.json

# ── SHARD: Debug/temp scripts in root ─────────────────────────────────────────
debug_*.py
tmp_*.py
temp_*.py
test_out*.txt
validation_*.log
ab_results_*.json
schemacallemilio.txt
ruvector.db
check_cuda.py
grep_trace.py
read_trace.py
hand_gesture_test.py
validate_ticket20.py

# ── SHARD: Root-level log/output txt files ────────────────────────────────────
output.txt
trace.txt
test_err.txt
test_results.txt
test_cad_out.txt
test_out.txt
test_out2.txt
specific_tests.txt
problemirealidishard.txt
shardmemorianuova.txt
tmp_design.txt
tmp_msgs.txt
roi_run_*.txt
roi_run_*.log
pytest_out*.txt
pytest_cad_out.txt

# ── SHARD: Local config (never commit) ────────────────────────────────────────
shard_tuning.json
.mcp.json
*.pdf

# ── SHARD: ChromaDB (binary, regenerable) ─────────────────────────────────────
shard_memory/chromadb/
shard_memory/*.bak

# ── SHARD: Visual input (large binary files) ──────────────────────────────────
shard_workspace/visual_input/
backend/face_landmarker.task

# ── SHARD: Benchmark forge artifacts ──────────────────────────────────────────
benchmark/task_forge_*/

# ── Tool artifacts (claude-flow, swarm) ───────────────────────────────────────
.claude-flow/
.swarm/

# ── SHARD: Root-level junk (sandbox cwd bug — continued) ──────────────────────
/'
/0.90
/2
/5%
/Machine
/SKIPPED
/float
/{ctx.hypothesis_confidence_updated

# ── SHARD: backend/ junk (sandbox cwd bug) ────────────────────────────────────
backend/'
backend/0.55
backend/0.8
backend/11
backend/120s
backend/2}
backend/3
backend/CERTIFY
backend/List[Dict]
backend/SNA
backend/new_relevance}
backend/pass_rate
backend/runs
backend/%s
backend/{ctx.hypothesis_confidence_updated
backend/2`.,+Commit
backend/shard_memory/
